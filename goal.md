# 가계부 웹앱 제작 목표

## 1. 프로젝트 목표

현재 프로젝트 폴더에 있는 `가계부 - Google Sheets.lnk`의 가계부 운영 로직을 웹앱 형태로 옮긴다. 최종 결과물은 단순한 “표 입력 화면”이 아니라, 매일 쓰기 편하고 월별 흐름을 한눈에 읽을 수 있는 개인 가계부 제품처럼 만든다.

디자인은 `DESIGN.md`를 기준으로 하며, Steep.app의 전반적인 인상처럼 흰 종이 위에 떠 있는 분석 카드, 큰 세리프 헤드라인, 절제된 흑백 UI, 드문 peach accent를 사용한다.

## 2. 현재 확인된 자료

- `DESIGN.md`
  - Steep 스타일 레퍼런스 문서
  - 색상, 타이포그래피, 카드, 버튼, floating artifact 스타일 규칙 포함
- `가계부 - Google Sheets.lnk`
  - Google Sheets 문서 바로가기
  - 문서 URL은 확인됨
  - 현재 직접 다운로드는 Google 권한 문제로 차단됨

따라서 초기 구현은 “스프레드시트 로직을 최대한 재현할 수 있는 구조”로 설계하고, 실제 시트 내부의 컬럼/수식/분류 체계는 접근이 가능해지는 즉시 반영한다.

## 3. 만들 웹앱의 핵심 방향

### 제품 컨셉

“개인 지출을 기록하는 앱”보다 한 단계 더 나아가, 내 돈의 흐름을 조용하고 명확하게 설명해주는 분석형 가계부로 만든다.

앱은 다음 질문에 빠르게 답할 수 있어야 한다.

- 이번 달에 얼마를 썼는가?
- 고정비와 변동비는 각각 얼마나 되는가?
- 예산 대비 얼마나 남았는가?
- 어떤 카테고리에서 지출이 늘었는가?
- 최근 기록해야 할 거래는 무엇인가?
- 월말 기준으로 소비 패턴이 안정적인가, 위험한가?

### 화면 톤

가계부이지만 은행 앱처럼 무겁거나, 회계 프로그램처럼 딱딱하지 않게 만든다. Steep처럼 제품 UI 조각들이 floating card로 떠 있고, 큰 문장형 헤드라인이 “현재 재정 상태”를 설명하는 느낌을 목표로 한다.

예시 톤:

> 이번 달 지출은 계획보다 조금 빠르게 움직이고 있어요.

## 4. 주요 기능 범위

### 4.1 대시보드

첫 화면에서 이번 달 상태를 요약한다.

- 이번 달 총수입
- 이번 달 총지출
- 예산 대비 남은 금액
- 저축률 또는 잔여율
- 고정비 / 변동비 비중
- 최근 거래 내역
- 카테고리별 지출 순위
- 월별 지출 추이

디자인은 일반적인 관리자 대시보드처럼 꽉 찬 그리드가 아니라, Steep의 “floating product artifact”처럼 여러 분석 카드가 여백 속에 배치되는 구조를 사용한다.

### 4.2 거래 입력

사용자가 직접 수입/지출을 추가할 수 있는 입력 영역을 둔다.

필드 초안:

- 날짜
- 유형: 수입 / 지출 / 이체
- 카테고리
- 결제수단 또는 계좌
- 금액
- 메모
- 고정비 여부

초기 버전에서는 브라우저 로컬 저장소 기반으로 구현하고, 이후 필요하면 Google Sheets 연동 또는 데이터베이스 저장으로 확장한다.

### 4.3 거래 목록

입력된 거래를 표 형태로 보여준다.

필요 기능:

- 월별 필터
- 유형별 필터
- 카테고리별 필터
- 검색
- 거래 수정
- 거래 삭제

표는 딱딱한 spreadsheet 복제보다, Steep의 region table card처럼 가볍고 정돈된 floating table로 만든다.

### 4.4 예산 관리

카테고리별 월 예산을 설정하고, 실제 사용액과 비교한다.

예시 카테고리:

- 식비
- 카페/간식
- 교통
- 주거
- 통신
- 구독
- 쇼핑
- 의료
- 여가
- 기타

각 카테고리는 다음 값을 가진다.

- 월 예산
- 현재 사용액
- 남은 금액
- 사용률

### 4.5 분석 카드

Steep 스타일에 맞춰 작고 선명한 분석 카드들을 배치한다.

예시 카드:

- “식비가 지난달보다 12% 증가”
- “고정비가 전체 지출의 42%”
- “이번 달 잔여 예산 318,000원”
- “최근 7일 평균 지출 24,500원”

차트는 복잡한 축과 격자를 줄이고, 간결한 선 그래프 / 막대 / ring progress 형태를 사용한다.

## 5. 데이터 모델 초안

### Transaction

```ts
type Transaction = {
  id: string;
  date: string;
  type: "income" | "expense" | "transfer";
  category: string;
  account: string;
  amount: number;
  memo?: string;
  isFixed?: boolean;
};
```

### Budget

```ts
type Budget = {
  category: string;
  monthlyLimit: number;
};
```

### Account

```ts
type Account = {
  id: string;
  name: string;
  type: "cash" | "bank" | "card" | "savings";
  balance?: number;
};
```

## 6. Google Sheets 로직 반영 계획

Google Sheets 접근이 가능해지면 다음 순서로 기존 로직을 웹앱에 옮긴다.

1. 시트 탭 구조 확인
2. 거래 입력 컬럼 확인
3. 카테고리/계정/결제수단 목록 확인
4. 월별 집계 수식 확인
5. 예산 계산 방식 확인
6. 기존 시트에서 중요하게 쓰던 지표 식별
7. 웹앱 데이터 모델과 계산 함수에 반영
8. 샘플 데이터로 결과값 대조

권한 때문에 시트를 바로 내려받을 수 없는 경우, 사용자가 `.xlsx`로 내보낸 파일을 프로젝트 폴더에 넣어주면 그 파일을 기준으로 로직을 추출한다.

## 7. 디자인 원칙

`DESIGN.md`를 우선 준수한다.

### 색상

- 배경: `#ffffff`
- 기본 텍스트: `#17191c`
- 카드 배경: `#f2f2f3`
- 보조 배경: `#fafafb`
- 보조 텍스트: `#777b86`
- accent: `#fbe1d1`
- accent 텍스트: `#5d2a1a`

### 형태

- 메인 카드 radius: `24px`
- floating artifact radius: `20px`
- 입력창 radius: `16px`
- 버튼 radius: `9999px`

### 타이포그래피

- 큰 제목은 세리프 계열
- UI와 본문은 sans-serif
- 과한 bold 사용 금지
- 큰 제목은 여백과 함께 editorial하게 배치

### 인터랙션 톤

- 입력과 수정은 빠르고 명확하게
- 시각 효과는 과하지 않게
- hover, focus 상태는 hairline border와 subtle background 정도로 표현

## 8. 초기 화면 구성안

### 상단 네비게이션

- 좌측: 앱 로고 / 이름
- 중앙: Overview, Transactions, Budgets, Insights
- 우측: 이번 달 선택, 새 거래 버튼

### Hero / Overview

- 큰 세리프 문장형 헤드라인
- 이번 달 상태 요약
- filled pill button: 거래 추가
- ghost pill button: 예산 보기

### Floating cards

- 월 지출 summary card
- budget progress card
- category ranking table
- recent transactions composer/table
- monthly trend mini chart

### 아래 섹션

- 거래 목록
- 카테고리 예산 현황
- 분석 인사이트
- 데이터 관리 영역

## 9. 구현 전략

### 1차 구현

- 프로젝트 초기화
- `DESIGN.md` 기반 CSS token 구성
- 단일 페이지 웹앱 제작
- 샘플 데이터 기반 대시보드/거래/예산/분석 구현
- 로컬 저장소에 거래 저장
- 반응형 레이아웃 적용

### 2차 구현

- Google Sheets 실제 구조 반영
- 기존 수식/카테고리/월별 계산 로직 이식
- 가져오기/내보내기 기능 추가
- 실제 사용 데이터와 대조 검증

### 3차 구현

- 필요 시 Google Sheets API, CSV import, 또는 Sites 저장소 기반 영구 저장 확장
- 로그인/개인 데이터 보호 흐름 검토
- 월별 리포트/인사이트 고도화

## 10. 완료 기준

초기 버전은 다음 조건을 만족하면 완료로 본다.

- `DESIGN.md`의 시각 규칙을 반영한 웹앱 UI가 있다.
- 대시보드에서 월별 수입/지출/예산 상태가 보인다.
- 거래를 추가, 수정, 삭제할 수 있다.
- 카테고리별 예산과 실제 사용액을 비교할 수 있다.
- 샘플 데이터 없이도 사용자가 직접 입력을 시작할 수 있다.
- 모바일에서도 주요 기능을 사용할 수 있다.
- 향후 Google Sheets 로직을 연결하기 쉬운 데이터 구조로 되어 있다.

## 11. 현재 리스크와 결정 필요 사항

### Google Sheets 접근

현재 바로가기 URL은 확인했지만 권한 문제로 직접 파일 내용을 읽을 수 없다. 실제 로직을 완전히 동일하게 옮기려면 다음 중 하나가 필요하다.

- Google Sheets 파일을 `.xlsx`로 내려받아 프로젝트 폴더에 넣기
- 시트 공유 권한을 읽기 가능하게 변경
- 주요 탭/컬럼/수식을 스크린샷 또는 텍스트로 제공

### 저장 방식

초기 버전은 로컬 저장소 기반으로 만든다. 이 방식은 빠르고 개인 사용에는 충분하지만, 여러 기기 간 동기화는 되지 않는다. 동기화가 필요하면 이후 Google Sheets 연동 또는 서버 저장 방식으로 확장한다.

## 12. 한 줄 요약

기존 Google Sheets 가계부의 계산 로직을 보존하되, 사용 경험은 Steep 스타일의 조용하고 아름다운 분석형 웹앱으로 재구성한다.
# 대시보드 통장 필터 변경 사항

첫 대시보드의 **이번 달 상태 요약**은 통장 선택 범위에 따라 계산한다. 기본값은 `전체 통장`이며, 사용자는 은행 계좌·카드·현금·저축 계좌를 **복수 선택**할 수 있다.

- 복수 선택한 통장의 거래는 하나의 범위로 합산한다.
- 선택하지 않은 통장의 거래는 수입·지출·잔여 예산·고정비 비중·최근 거래·카테고리 순위·월별 추이·인사이트에서 제외한다.
- `전체 통장` 선택 시 모든 통장 데이터를 표시한다.
- 현재 선택값을 카드 제목과 헤드라인에 표시한다. 예: `국민 주거래 · 신한 체크카드 기준`.
- 선택 상태는 새로고침 뒤에도 유지한다.

구현 시 대시보드 공통 필터는 다음처럼 관리한다.

```ts
type DashboardFilter = {
  month: string;
  selectedAccountIds: string[]; // 빈 배열은 전체 통장
};
```

---
